# A simple way of padding numbers? Nope. -- Ricky26
KMDF_VERSION_STRING=$(KMDF_VERSION_MINOR)

!if $(KMDF_VERSION_MINOR) < 10
KMDF_VERSION_STRING=0$(KMDF_VERSION_STRING)
!endif

!if $(KMDF_VERSION_MINOR) < 100
KMDF_VERSION_STRING=0$(KMDF_VERSION_STRING)
!endif

KMDF_VERSION_STRING=$(KMDF_VERSION_MAJOR)$(KMDF_VERSION_STRING)

!if $(KMDF_VERSION_MAJOR) < 10
KMDF_VERSION_STRING=0$(KMDF_VERSION_STRING)
!endif

KMDF_COINSTALLER_NAME=WdfCoInstaller$(KMDF_VERSION_STRING).dll
KMDF_COINSTALLER_SRC=$(BASEDIR)\redist\wdf\$(_BUILDARCH)\$(KMDF_COINSTALLER_NAME)

